kind: ExternalService
name: reminder
spec:
  env:
    - name: VERSION
      value: ${TAG}
    - name: MONGO_URI
      secret: mongo-uri
    - name: BOT_TOKEN
      secret: bot-token
  allow_http: true
  image: ${IMAGE_URL}:${RELEASE_VERSION}
  image_pull_policy: IfNotPresent
  path: /
  replicas: 1
  resources:
    memory: 200Mi